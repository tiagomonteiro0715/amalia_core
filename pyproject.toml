[project]
name = "amalia"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.0",
    "torch>=2.12.1",
]

[tool.pyright]
extraPaths = ["amalia-core"]
